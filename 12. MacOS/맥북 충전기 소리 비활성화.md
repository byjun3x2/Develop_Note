---
link: "[[_MacOS_Start]]"
title: 맥북 충전기 소리 비활성화Google Gemini
source: https://gemini.google.com/app/1f313b9720df6c58?hl=ko
created: 2025-12-05
tags:
  - clippings
---

### 1\. 충전 연결음 끄기 (Disable)

Bash

```
defaults write com.apple.PowerChime ChimeOnAllHardware -bool false; killall PowerChime
```

- **설명:**`defaults write` 로 설정 값을 끄고(`false`), `killall` 로 현재 실행 중인 사운드 프로세스를 종료시키는 명령어

---

### 2\. 충전 연결음 다시 켜기 (Enable)

Bash

```
defaults write com.apple.PowerChime ChimeOnAllHardware -bool true; open /System/Library/CoreServices/PowerChime.app
```

